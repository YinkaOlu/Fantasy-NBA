<% var menu = { 'TEAM CRUD': '/teamform', 'PLAYER CRUD': '/playerform', 'GAME CRUD': '/gameform'}; %>

<div class="navbar navbar-default">
    <div class="container-fluid">
        <div class="navbar-header" >
            <ul class="nav navbar-nav">
                <% for(var key in menu) { %>
                <li><a class="brand" href="<%= menu[key] %>"><%= key %></a></li>
                <% } %>
            </ul>
        </div>
    </div>
</div>